# address of the backend api, without this the frontend cannot reach the server
VITE_API_BASE_URL=http://localhost:8000

# google login is optional, leave empty if you are not using it
VITE_GOOGLE_CLIENT_ID=
